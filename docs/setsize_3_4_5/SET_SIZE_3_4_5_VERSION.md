# 3x3 / 4x4 / 5x5 set size test version

This folder is a copied test version of `online_experiment`.

Changes from the current formal version:

- `SET_SIZES` changed from `[3, 5, 7]` to `[3, 4, 5]`.
- Set-size counterbalancing orders changed to the six complete permutations of `3, 4, 5`.
- Experiment version changed to `numeric-audit-cue-validity-1.10.43-setsize-3-4-5`.

The original `online_experiment` folder is not modified by this copy.
